---
import Layout from "../layouts/Layout.astro";
import HeaderLogo from "../components/HeaderLogo.astro";
import KinkCheck from "../components/KinkCheck";
import RatingOverview from "../components/RatingOverview.astro";
import { db, Template } from "astro:db";
import type { kinklist } from "../base";

const template = await db
	.select()
	.from(Template)
	.then((x) => x[0]);
---

<Layout>
	<header>
		<HeaderLogo />
		<RatingOverview />
	</header>
	<KinkCheck kinks={template.data as kinklist} client:load />
</Layout>

<style>
	:global(div#content) {
		min-width: 475px;
		max-width: 1800px;
	}
	header {
		display: flex;
		flex-direction: row;
		justify-content: space-between;
	}
</style>
